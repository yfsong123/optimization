#:include connected.kv
#:include forgot.kv
#:include mainpage.kv
#:include firstpage.kv
#:include newspages.kv

<Login>:
    BoxLayout
        id: login_layout
        orientation: 'vertical'
        padding: [10,50,10,50]
        spacing: 50
        
        canvas.before:
            Rectangle:
                pos: self.pos
                size: self.size
                source: './figures/background.jpg'
        Label:
            text: 'Welcome to Portfolio Optimizer'
            font_size: self.width *0.06
            
        BoxLayout:
            orientation: 'vertical'

            Label:
                text: 'Username'
                font_size: self.width *0.02
                halign: 'left'
                text_size: self.size

            TextInput:
                id: login
                multiline:False
                font_size: self.width *0.02
                size_hint: 1, 0.5

        BoxLayout:
            orientation: 'vertical'
            Label:
                text: 'Password'
                halign: 'left'
                font_size: self.width *0.02
                text_size: self.size

            TextInput:
                id: password
                multiline:False
                password:True
                font_size: self.width *0.02
                size_hint: 1, 0.5

        Button:
            text: 'Sign in'
            size_hint: 0.2, 0.3
            pos_hint: {'x': 0.4, 'y':0.5}
            font_size: self.width *0.1
            color: [0,0,1,1]

            on_press: root.do_login(login.text, password.text)
            
            
        BoxLayout:
            orientation: 'horizontal'
            Button:
                text: 'Forget Username/Password?'
                size_hint: 0.2, 0.3
                pos_hint: {'x': 0.4, 'y':0.5}
                font_size: self.width *0.05
                color: [1,0,0,1]
                background_color : [0,0,0,0.0]
                on_press: root.forgot()
                
            Button:
                text: 'Not Enrolled? Sign Up Now'
                size_hint: 0.2, 0.3
                pos_hint: {'x': 0.4, 'y':0.5}
                font_size: self.width *0.05
                color: [1,0,0,1]
                background_color : [0,0,0,0.0]